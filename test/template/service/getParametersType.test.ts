import { getParametersType } from '../../../src/template/service/getParametersType'

describe('getParametersType', () => {
  it('should return 15 for add(10,5)', () => {
    // expect(getParametersType([])).toBe(15)
  })
})
